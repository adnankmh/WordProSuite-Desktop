# WordPro Suite Desktop Pro v2 — الميزات

إجمالي الأدوات المسجلة: **206 أداة** موزعة على **10 قسمًا**.

## العربية والاتجاه — 18 أداة
- اتجاه RTL (`rtl`)
- اتجاه LTR (`ltr`)
- اكتشاف الاتجاه تلقائيًا (`auto-direction`)
- حذف التشكيل (`remove-diacritics`)
- حذف التطويل (`remove-tatweel`)
- توحيد الحروف العربية (`normalize-arabic`)
- ترقيم عربي ذكي (`normalize-arabic-punctuation`)
- علامات اقتباس عربية (`arabic-quotes`)
- حذف المحارف المخفية (`remove-zero-width`)
- أرقام عربية (`digits-eastern`)
- أرقام غربية (`digits-western`)
- تصحيح كتابة عربية إلى إنجليزية (`keyboard-ar-to-en`)
- تصحيح كتابة إنجليزية إلى عربية (`keyboard-en-to-ar`)
- إدراج التاريخ بالعربية (`insert-arabic-date`)
- إدراج التاريخ الهجري (`insert-hijri-date`)
- عكس النص (`reverse-text`)
- رقم إلى كلمات عربية (`number-arabic-words`)
- تنظيف عربي شامل (`arabic-clean-all`)

## النص والتحرير — 31 أداة
- تنظيف المسافات (`collapse-spaces`)
- قص حواف الأسطر (`trim-lines`)
- حذف الفقرات الفارغة (`remove-empty-paragraphs`)
- دمج الفقرات (`join-paragraphs`)
- Tabs إلى مسافات (`tabs-to-spaces`)
- فواصل الأسطر إلى فقرات (`linebreaks-to-paragraphs`)
- حذف الأسطر المكررة (`remove-duplicate-lines`)
- حذف الفقرات المكررة (`remove-duplicate-paragraphs`)
- حذف الكلمات المتتابعة المكررة (`remove-duplicate-words`)
- فرز تصاعدي (`sort-lines-asc`)
- فرز تنازلي (`sort-lines-desc`)
- إضافة بادئة للأسطر (`prefix-lines`)
- إضافة لاحقة للأسطر (`suffix-lines`)
- تقسيم حسب فاصل (`split-by-delimiter`)
- تحويل إلى نقاط (`paragraphs-bullets`)
- تحويل إلى ترقيم (`paragraphs-numbers`)
- إزالة تنسيق القائمة (`clear-list`)
- UPPERCASE (`uppercase`)
- lowercase (`lowercase`)
- Title Case (`titlecase`)
- Sentence case (`sentence-case`)
- Invert Case (`invert-case`)
- اقتباس ذكي (`smart-quotes`)
- توحيد الشرطات (`normalize-dashes`)
- حذف المحارف غير المطبوعة (`remove-nonprinting`)
- إزالة الروابط (`remove-hyperlinks`)
- استخراج البريد الإلكتروني (`extract-emails`)
- استخراج الروابط (`extract-urls`)
- استخراج أرقام الهواتف (`extract-phones`)
- إحصاءات النص (`text-statistics`)
- لصق كنص فقط (`paste-plain`)

## التنسيق والأنماط — 33 أداة
- Arial 14 (`font-arial-14`)
- Tahoma 12 (`font-tahoma-12`)
- Times New Roman 12 (`font-times-12`)
- Calibri 11 (`font-calibri-11`)
- إزالة التنسيق المباشر (`clear-formatting`)
- محاذاة يمين (`align-right`)
- محاذاة يسار (`align-left`)
- توسيط (`align-center`)
- ضبط كامل (`align-justify`)
- تباعد مفرد (`line-spacing-1`)
- تباعد 1.15 (`line-spacing-115`)
- تباعد 1.5 (`line-spacing-15`)
- تباعد مزدوج (`line-spacing-2`)
- مسافة بعد 0 (`space-after-0`)
- مسافة بعد 6 (`space-after-6`)
- مسافة بعد 12 (`space-after-12`)
- مسافة بادئة للسطر الأول (`indent-first-line`)
- إزالة المسافات البادئة (`indent-remove`)
- إبقاء مع التالي (`keep-with-next`)
- بدء الفقرة في صفحة جديدة (`page-break-before`)
- تمييز أصفر (`highlight-yellow`)
- إزالة التمييز (`highlight-clear`)
- إطار حول النص (`border-box`)
- إزالة الحدود (`border-clear`)
- عنوان 1 (`heading-1`)
- عنوان 2 (`heading-2`)
- عنوان 3 (`heading-3`)
- نمط عادي (`style-normal`)
- تقرير عربي (`format-arabic-report`)
- خطاب رسمي (`format-official-letter`)
- تنسيق أكاديمي (`format-academic`)
- حفظ لقطة تنسيق (`format-copy`)
- تطبيق لقطة التنسيق (`format-apply`)

## الجداول — 35 أداة
- جدول RTL (`table-rtl`)
- جدول LTR (`table-ltr`)
- تنسيق جدول احترافي (`table-professional`)
- تنسيق صف العنوان (`table-header-style`)
- صفوف متبادلة (`table-banded-rows`)
- ملاءمة الصفحة (`table-autofit-window`)
- ملاءمة المحتوى (`table-autofit-content`)
- توزيع الأعمدة (`table-distribute-columns`)
- توزيع الصفوف (`table-distribute-rows`)
- تكرار صف العنوان (`table-repeat-header`)
- حذف الصفوف الفارغة (`table-remove-empty-rows`)
- حذف الصفوف المكررة (`table-remove-duplicate-rows`)
- تنظيف الخلايا (`table-trim-cells`)
- توسيط الخلايا (`table-center-cells`)
- ترقيم العمود الأول (`table-number-first-column`)
- مجموع العمود (`table-sum-column`)
- متوسط العمود (`table-average-column`)
- فرز الجدول تصاعديًا (`table-sort-asc`)
- فرز الجدول تنازليًا (`table-sort-desc`)
- صف أعلى (`table-row-above`)
- صف أسفل (`table-row-below`)
- عمود يسار (`table-col-left`)
- عمود يمين (`table-col-right`)
- حذف الصف (`table-delete-row`)
- حذف العمود (`table-delete-column`)
- دمج الخلايا (`table-merge-cells`)
- تقسيم الخلية (`table-split-cell`)
- كل الحدود (`table-borders-all`)
- بدون حدود (`table-borders-none`)
- محاذاة عمودية أعلى (`table-vertical-top`)
- محاذاة عمودية وسط (`table-vertical-middle`)
- محاذاة عمودية أسفل (`table-vertical-bottom`)
- تحويل نص إلى جدول (`text-to-table`)
- تحويل جدول إلى نص (`table-to-text`)
- تبديل الصفوف والأعمدة (`table-transpose`)

## المستند والصفحات — 34 أداة
- تحديث جميع الحقول (`update-fields`)
- إضافة فهرس محتويات (`insert-toc`)
- إضافة أرقام الصفحات (`page-numbers-add`)
- حذف أرقام الصفحات (`page-numbers-remove`)
- فاصل صفحة (`insert-page-break`)
- مقطع صفحة جديدة (`insert-section-next`)
- مقطع مستمر (`insert-section-continuous`)
- حذف فواصل الصفحات (`remove-page-breaks`)
- حذف فواصل المقاطع (`remove-section-breaks`)
- حجم A4 (`page-a4`)
- حجم A3 (`page-a3`)
- حجم Letter (`page-letter`)
- اتجاه عمودي (`page-portrait`)
- اتجاه أفقي (`page-landscape`)
- هوامش عادية (`margins-normal`)
- هوامش ضيقة (`margins-narrow`)
- عمود واحد (`columns-1`)
- عمودان (`columns-2`)
- ثلاثة أعمدة (`columns-3`)
- تشغيل أرقام الأسطر (`line-numbers-on`)
- إيقاف أرقام الأسطر (`line-numbers-off`)
- نص رأس الصفحة (`header-text`)
- نص تذييل الصفحة (`footer-text`)
- مسح الرؤوس والتذييلات (`clear-headers-footers`)
- إدراج تاريخ ووقت (`insert-date-time`)
- إدراج اسم ومسار الملف (`insert-file-name`)
- إحصاءات المستند (`document-statistics`)
- قائمة خطوط الأنماط (`list-fonts`)
- استبدال خط (`replace-font`)
- حذف النص المخفي (`remove-hidden-text`)
- تحويل الحقول إلى نص (`unlink-fields`)
- حذف الإشارات المرجعية (`remove-bookmarks`)
- إنشاء صفحة غلاف (`insert-cover`)
- كتلة توقيع (`insert-signature`)

## المراجعة والأمان — 13 أداة
- تشغيل تعقب التغييرات (`track-on`)
- إيقاف تعقب التغييرات (`track-off`)
- حذف التعليقات (`remove-comments`)
- قبول كل التعديلات (`accept-revisions`)
- رفض كل التعديلات (`reject-revisions`)
- حماية للقراءة فقط (`protect-readonly`)
- حماية تعبئة النماذج (`protect-forms`)
- إلغاء الحماية (`unprotect`)
- إزالة البيانات الشخصية (`remove-metadata`)
- تنظيف الخصائص المخصصة (`clean-properties`)
- فصل الروابط الخارجية (`remove-external-links`)
- تجهيز نسخة نهائية (`workflow-final-delivery`)
- تنظيف شامل قبل المشاركة (`workflow-final-share`)

## الملفات وPDF — 13 أداة
- نسخة احتياطية مؤرخة (`save-backup`)
- حفظ إصدار جديد (`save-version`)
- نسخة DOCX (`save-docx-copy`)
- حفظ كل المستندات (`save-all`)
- تصدير PDF (`export-pdf`)
- تصدير PDF/A (`export-pdfa`)
- تصدير التحديد PDF (`export-selection-pdf`)
- كل مقطع PDF مستقل (`export-sections-pdf`)
- تحويل مجلد إلى PDF (`batch-pdf`)
- دمج مستندات (`merge-documents`)
- فتح موقع المستند (`open-document-folder`)
- نسخ مسار المستند (`copy-document-path`)
- حزمة أرشيف (`archive-package`)

## القوالب — 18 أداة
- جدول أعمال (`template-agenda`)
- محضر اجتماع (`template-minutes`)
- خطاب رسمي (`template-official-letter`)
- تقرير طبي (`template-medical-report`)
- تقرير مشروع (`template-project-report`)
- تسليم واستلام (`template-handover`)
- مذكرة داخلية (`template-memo`)
- عرض سعر (`template-quotation`)
- فاتورة (`template-invoice`)
- شهادة تقدير (`template-certificate`)
- كشف حضور (`template-attendance`)
- شروط مرجعية ToR (`template-tor`)
- مذكرة مفاهيمية (`template-concept-note`)
- تقرير جهة مانحة (`template-donor-report`)
- إجراء تشغيلي SOP (`template-sop`)
- سجل مخاطر (`template-risk-register`)
- خطة عمل (`template-action-plan`)
- خطاب تغطية (`template-cover-letter`)

## الأدوات الذكية — 5 أداة
- حاسبة التحديد (`calculator`)
- إدراج UUID (`insert-guid`)
- نص عربي تجريبي (`insert-lorem-arabic`)
- قائمة مهام بمربعات (`insert-checklist`)
- فتح سجل الإضافة (`open-log`)

## النظام والترخيص — 6 أداة
- مركز الأدوات الاحترافي (`command-center`)
- تفعيل البرنامج (`activate`)
- حالة الترخيص (`license-info`)
- نسخ معرّف الجهاز (`machine-id`)
- فحص الإضافة (`health-check`)
- حول WordPro Suite (`about`)
